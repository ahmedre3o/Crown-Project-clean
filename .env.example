# Crown frontend – API base URL (baked at build time)
# Production: backend direct (no proxy). Local dev can use http://localhost:5001/api or leave unset to use fallback.
NEXT_PUBLIC_API_URL=https://api.crowncs.org/api
